<div class="comment{{#if isDeep}} comment_deep{{/if}}" data-comment-id="{{ id }}" data-trigger="reply:{{ id }}">
	<div class="comment__h">
		<span class="avatar comment__avatar">{{#if hash}}<img src="http://www.gravatar.com/avatar/{{ hash }}?s=50" class="comment__avatar-img" />{{/if}}</span>
		<h3 class="comment__author">{{ author.name }}</h3>
		<p class="comment__date">{{ date }}</p>
	</div>
	<div class="comment__content">{{{ content }}}</div>
	{{#each children}}
		{{renderComment .}}
	{{/each}}
</div>